# 期货交易分析系统 — 接口核查报告

核查日期：2026-09-07（2026-09-08 补充核查大商所持仓排名数据源，见 B8）
核查范围：后端 FastAPI 路由（backend/routers、services、modules、agents/tools）与前端（frontend/src）之间的接口使用关系，以及各取数落盘模块的静态正确性。核对对象包括 `backend/data/qihuo/database` 下的真实 CSV。

结论分两大类：**A. 实现了但从未被调用**；**B. 取数/返回数据有错误**。

---

## A. 实现了但没有被调用

### A1. 前端封装了 API，但任何组件都不调用（对应后端端点无人使用）

前端 `frontend/src/api/index.js` 中定义了以下方法，但在 `src/views`、`src/components` 里 **0 次引用**：

| # | 前端封装（api/index.js） | 对应后端端点 | 说明 |
|---|---|---|---|
| 1 | `systemApi.getCommodities()` (31) | GET `/api/system/commodities` | 从未调用；与 `/api/data/commodities` 重复且数据口径不一致（见 B3） |
| 2 | `systemApi.health()` (32) | GET `/api/system/health` | UI 不调用（仅 start.js/容器健康检查使用，属合理保留） |
| 3 | `dataApi.getDominantContracts()` (39) | GET `/api/data/dominant-contracts` | 从未调用；`/data/commodities` 每行已含 `dominant_contract`，该端点冗余 |
| 4 | `analysisApi.getProgress()` (51) | GET `/api/analysis/progress` | 从未调用；页面统一轮询 `/analysis/status` |
| 5 | `analysisApi.getResult(commodity)` (52) | GET `/api/analysis/results/{commodity}` | 从未调用 |
| 6 | `analysisApi.deleteCache()` (55) | DELETE `/api/analysis/cache/{commodity}/{date}` | 从未调用；缓存列表只有“加载”，**缓存只能增加无法从 UI 删除** |
| 7 | `scheduledApi.getStatus()` (68) | GET `/api/scheduled/status` | 从未调用；运行状态靠 `/config` 的 `enabled` 判断 |

同时 Pinia store（frontend/src/stores/app.js）内两个 action **无任何视图调用**：
- `fetchSystemStatus` (54, 导出 144)：`SystemStatus.vue:50` 绕过 store 直接调 `systemApi.getStatus()`；
- `updateData` (97, 导出 149)：`DataUpdate.vue:213` 直接调 `dataApi.updateData()`。

### A2. `/api/data/varieties/{module}` 实际只在“第一个模块”上被调用一次

`frontend/src/views/DataManagement.vue`：
- 折叠面板 `activeModules = ['inventory']` (97)，面板没有 `@change` 事件（42-84）；
- `onMounted` 里只 `loadVarieties(keys[0])`（148-153），`keys[0]` = 后端 MODULE_CONFIG 第一个 key = `inventory`；
- 品种表格由 `v-if="varieties[key]?.length"` 控制（72）。

结果：用户展开 positioning / term_structure / technical_analysis / basis / receipt 面板时**不会触发任何请求**，`varieties[key]` 始终为 undefined，表格永远空白。即该端点 5/6 的模块数据等于“有接口、没有调用入口”。

### A3. 后端 Python 实现但全库无调用者的函数

| 位置 | 内容 |
|---|---|
| modules/news.py:260 `get_contract_news` / :336 `get_market_sentiment` | 迁移后遗留代码，无人调用（现入口为 `search_contract_news`） |
| services/scheduled_service.py:43 `should_catch_up` | 定时“补跑”逻辑从未接入主循环 |
| services/commodity_service.py:121 `get_symbols_by_exchange` | 定义后无调用 |
| core/settings.py:84-109 `get_data_path/get_results_path/get_cache_path/get_all_symbols/get_symbol_name` | 均无调用，路由直接读 `settings.*` |
| models 中 `CommodityDataStatus`(analysis.py:86)、`CommodityVarietyInfo`(data.py:32)、`WordReportRequest`(data.py:47)、`SystemStatusResponse`(data.py:53) | 定义了并 export，但从未作为 response_model/请求模型使用 |

### A4. 配置项“提交了但从不生效”的开关

后端 **没有任何代码消费**以下开关（已在全后端 *.py 中确认）：

- `use_realtime`（实时数据）：analysis_service.py 只在 submit_task 里把值存进 `task.config`（51），之后再无读取；手动分析、定时分析都不产生行为差异。
- `auto_email`、`auto_word`、`update_data_before_analysis`：`models/analysis.py:114-116` 只声明为 `ScheduledConfig` 字段；定时触发 `scheduled_service._on_trigger()`（85-93）只消费 commodities/analysis_date/modules/analysis_mode/ai_model/use_realtime/debate_rounds——**这三个选项被 UI 收集、被 /scheduled/start 存储，却没有任何执行路径**（不开邮件/Word、也不会先更新数据）。

前端位置：AnalysisConfig.vue:73(useRealtime)、143(update_data_before_analysis)、146(auto_email)、149(auto_word)。

---

## B. 取数/返回数据有错误的接口

### B1. 新闻检索实际上永远取不到数据（配置找不到）

- `modules/news.py:14` 密钥读环境变量 `TIAN_API_KEY`（.env/.env.example/docker-compose 均未配置）；
- `load_config()` (56-61) 用 **CWD 相对路径** `config/news_config.json`，文件不存在直接返回 `{"enabled": False}`，`search_contract_news` 返回空。
- 仓库里该配置只存在于**仓库根** `config/news_config.json`，而 `backend/config/` 下没有；
  - 本地：start.js:145-151 以 `cwd=BACKEND_DIR` 启动 → 找的是 `backend/config/news_config.json` → 不存在；
  - Docker：backend/Dockerfile WORKDIR=/app，build context 是 `./backend` → 根目录的 news_config.json 根本没进镜像。
- 后果：新闻模块（辩论阶段 `search_news_data` 工具）在默认部署下**必然为空**，即便 UI 勾选了 news 模块也拿不到任何新闻，与 news_search_migration.md“保留新闻检索”的意图相悖。

### B2. 技术面 CSV 存在两套 schema，接口把“品种代码”当“日期”解析

- `technical_updater.py` 有两条取数路径：
  - 新浪 `futures_main_sina` 主路径 → CSV 列序为 `时间,开盘,...`（EG/JD/PK 的 ohlc_data.csv 首行如此）；
  - `get_futures_daily` 兜底路径（209-258，`_process_get_futures_daily_data` 416-487）→ 产出的文件列序变成 **`symbol,date,开盘,...`**（本地 MA/ohlc_data.csv 首行：`symbol,date,开盘,最高,最低,收盘,成交量,持仓量,turnover,settle,pre_settle,v`，数据行首列恒为 `MA610`）。
- 消费方一律假设“第一列=日期”：
  - `/api/data/varieties`：`start_date = lines[1].split(",")[0]`、`end_date = lines[-1].split(",")[0]`（data_service.py:170-172）→ MA 返回的起始/结束日期实际是 **“MA610”**；
  - 分析模块 `latest_date = df.iloc[-1][df.columns[0]]`（analysis_service.py:213-214）→ MA 的 `latest_date` 同样错误。
- 结论：`/api/data/varieties/technical_analysis` 与模块状态里的“最新日期”对 MA（以及走同路径的 CZCE 品种）返回错误数据。

### B3. 期限结构对 DCE 品种取数错误（曲线塌缩成主力连续单行）

- `term_structure_updater.py:164-189`：DCE 分支用 `ak.futures_main_sina(symbol="EG0")` 拉**主力连续**，不是各月合约 → 每个交易日只有 1 行，无法构成期限结构；
- 其余交易所（CZCE/SHFE/INE/GFEX，:190-192）走 `ak.get_futures_daily`，是完整多合约曲线。
- 本地数据佐证：
  - `term_structure/MA/term_structure.csv`：单日多合约（MA2606~MA2708…）且 roll_yield 有值；
  - `term_structure/EG/term_structure.csv`：全部行 `symbol=EG0`、`roll_yield=0.0`，仅 65 行 → 曲线与展期收益率对 DCE 品种无意义。
- 另外该模块统一把日期写成 `YYYYMMDD`（如 20260608），与其它模块的 `YYYY-MM-DD` 不一致，数据管理页展示错乱。

### B4. 品种列表两套口径不一致（system vs data）

- GET `/api/system/commodities`（routers/system.py:29-35）返回 `settings.SUPPORTED_COMMODITIES`：**36 个硬编码**（settings.py:37-74），键是“黄金/铜”这类简称；
- GET `/api/data/commodities`（routers/data.py:21-24）读 commodities.yaml：**59 个**品种（config/commodities.yaml），名为“沪金/沪铜”。
- `/api/system/status` 里 `supported_commodities=36` 也与配置文件 59 不符。
- 结论：依赖 system 端口的消费方会拿到一份陈旧、不完整且命名不同的品种表。

### B5. 库存更新静默丢弃部分品种（不更新也不报错）

- `inventory_updater.py:18-32` 的 `SYMBOL_MAPPING` 约 52 项；59 个配置品种中的 `SC(原油)`、`BC(国际铜)` 等不在其中；
- 全量更新（`update_to_date(target_date, None)`，263-266）只遍历 SYMBOL_MAPPING → 上述品种永远不更新，且更新接口返回“成功”；
- 指定品种更新时 `[s for s in specific_varieties if s in SYMBOL_MAPPING]`（263）直接过滤 → 不在映射的品种既不更新、也不进 `failed_varieties`，UI 显示“更新成功”误导用户。

### B6. 前端状态标签与后端 key 体系不一致

- 后端 `check_commodity_data` 返回 MODULE_CONFIG 的 6 个 key：`inventory, positioning, term_structure, technical_analysis, basis, receipt`（data_service.py:179-184）；
- `AnalysisConfig.vue:196-203` 的 `moduleShortNames` 键为 `technical`（没有 `technical_analysis`）→ 数据检查里“技术面”标签渲染为空；勾选模块（187-194）用 `technical` 且不含 `receipt`，与返回 key 两套命名，标签/状态对不上。

### B7. 次要不一致

- GET `/api/analysis/results/{commodity}` 的缓存兜底只用“当天”日期（routers/analysis.py:56），历史日期即使有缓存也会 404（本接口当前无人调用，属于半成品）；
- `/word-report` 的 `include_charts` 参数前端永远传 false，服务端也未实现插图逻辑；
- GET `/api/data/dominant-contracts` 只返回 yaml 快照（约 51 条），与 `/data/commodities` 行内（快照+本地主力库回退补齐的 59 条）口径不同，且无人调用。

### B8. 大商所（DCE）持仓排名取数全挂——官网启用了动态 JS 反爬，新浪源已停更（2026-09-08 核查补充）

- `positioning_updater.py` 的大商所链路原本依赖 akshare 三个“直连 DCE 官网”的接口（`_fetch_dce_data_with_fallback`，positioning_updater.py:602）。2025 年起大商所官网全站被瑞数（RiverSecurity）动态 JS 反爬保护，普通 HTTP 请求只能拿到 **412 挑战页**（页面含 `$_ts` JS 脚本，需浏览器执行后才放行）：
  - 主接口 `ak.futures_dce_position_rank`（走 `exportMemberDealPosiQuotesBatchData.html`，按 zip 解析）→ 报 `File is not a zip file`（把 HTML 挑战页当 zip 解析失败）；
  - 备用接口 `ak.futures_dce_position_rank_other`（`memberDealPosiQuotes.html` 直接贴 Excel）→ HTML 解析拿不到表格，报 `list index out of range`；
  - 排名表接口 `ak.get_dce_rank_table` 与备用接口同一页面，永远打印“请使用 ak.futures_dce_position_rank()”而无数据。
  - 实测直接 GET 该页面返回 HTTP 412 + `$_ts` 动态 JS，确认是反爬而非接口变更。
- 新浪链路同样救不了 DCE：`qh_rankinglist` 只覆盖 CZCE/SHFE/INE/GFEX；实测 2026 年对 `jd2611` 等 DCE 合约表格为空（同页面 CZCE 的 `MA` 正常 21 行），且新浪对 DCE 的会员成交持仓数据 2024 年后已整体停更。
- 后果：运行日志（2026-09-07 实测）中 DCE 品种三个策略依次 `❌ 失败`，当日无任一 DCE 品种入库、日志报 `⚠️ 无数据`；而 `MA` 等郑商所品种走新浪源正常 —— 即持仓席位数据**缺了一整个交易所**，且系统仍显示“更新完成”，有误导性。
- 修复方案（东财数据中心镜像作为首选源，官网接口降级保留）详见“修复记录”末行。

---

## 建议修复优先级

P0（接口返回数据确实错误）：
1. 修复 news 配置读取（改为相对 backend 的绝对路径/或随代码打包，并配置 TIAN_API_KEY）；
2. technical_updater 统一 ohlc_data.csv 列序（都让首列为日期），或 data_service/analysis_service 读取时按列名取日期；
3. term_structure DCE 分支改走全合约数据源，避免 EG/JD/L 等只写主力连续单行。

P1（功能不可达）：
4. DataManagement 折叠面板绑定 @change 加载对应模块 varieties；
5. 给缓存列表补“删除”按钮（接线 `deleteCache`），否则 /api/analysis/cache 的 DELETE 形同虚设；
6. 明确 `auto_email/auto_word/update_data_before_analysis/use_realtime` 的实现或从前端移除，避免“配置了却不生效”；
7. 统一 `/api/system/commodities` 与 `/api/data/commodities` 品种来源（删除硬编码 SUPPORTED_COMMODITIES 或改为读 yaml）。

P2（清理冗余）：
8. 清理无调用函数（A3 清单）与未使用的前端封装（A1 清单）。

---

## 修复记录（2026-09-08）

针对上述问题已完成一轮全面修复，结论对照如下：

| 问题 | 修复动作 | 关键位置 |
|---|---|---|
| **B1** 新闻配置读取不到 | `load_config()` 改为按候选顺序查找：`NEWS_CONFIG_PATH` → 仓库根 `config/news_config.json` → `backend/config/news_config.json`（随镜像打包）→ CWD 相对路径；找不到即返回 disabled。已将根配置复制到 `backend/config/news_config.json`，并在 docker-compose 增加 `TIAN_API_KEY` 透传。 | backend/modules/news.py、backend/config/news_config.json、docker-compose.yml |
| **B1** TIAN_API_KEY 读取时机 | 由“import 时一次性读取”改为每次请求时经 `_get_tian_api_key()` 从环境变量读取（Windows/Linux 系统环境变量注入，不进代码库），缺失时给出明确日志而非崩溃。 | backend/modules/news.py |
| **B2** 技术面 CSV 双 schema | 写入侧：`get_futures_daily` 兜底路径返回前裁剪为标准列 `时间,开盘,最高,最低,收盘,成交量[,持仓量]`；`save_variety_data` 增加 `_standard_ohlc_columns()` 列归一化，合并时丢弃 symbol/date/turnover/settle/pre_settle/variety 等冗余列；历史脏文件在下次更新时自动重写清理。读取侧：`data_service.get_module_varieties` 与 `analysis_service` 改为**按列名**（时间/日期/date/trade_date）识别日期列，并将 `YYYYMMDD` 规范成 `YYYY-MM-DD` 展示，不再假定“第一列=日期”。 | backend/modules/technical_updater.py、backend/services/data_service.py、backend/services/analysis_service.py |
| **B3** DCE 期限结构取数 | DCE 分支与其它交易所统一改走 `ak.get_futures_daily(market=...)` 全合约日报（单日多合约），不再拉“主力连续 XX0”；并删除冗余的 DCE 品种循环。日期展示问题由读取侧日期规范化一并解决。 | backend/modules/term_structure_updater.py |
| **B4** 品种口径不一致 | `/api/system/status` 的 `supported_commodities` 与 `/api/system/commodities` 均改读 commodities.yaml（59 个），删除对硬编码 `SUPPORTED_COMMODITIES` 的依赖，并新增 `commodity_symbols`/`count` 字段。 | backend/routers/system.py |
| **B5** 库存静默丢品种 | 补齐 `SC→原油`、`BC→国际铜` 映射；指定品种更新时，无映射品种不再被静默过滤，而是写入 `failed_varieties` + 明确错误信息。 | backend/modules/inventory_updater.py |
| **B6** 前端标签 key 不一致 | `moduleShortNames` 补充 `technical_analysis→技术`、`news→新闻` 映射，数据检查标签不再空白。 | frontend/src/views/AnalysisConfig.vue |
| **A1** 缓存删除无入口 | 缓存列表增加“删除”按钮 + 二次确认，接线 `analysisApi.deleteCache`。 | frontend/src/views/AnalysisResults.vue |
| **A1** 重复实现/死封装 | `SystemStatus.vue`、`DataUpdate.vue` 改走 Pinia store 的 `fetchSystemStatus`/`updateData`（`updateData` 支持透传参数）；移除 0 引用的前端封装：`systemApi.getCommodities/health`、`dataApi.getDominantContracts`、`analysisApi.getProgress/getResult`、`scheduledApi.getStatus`（后端 `/api/system/health` 路由保留，供容器健康检查）。 | frontend/src/api/index.js、frontend/src/stores/app.js、frontend/src/components/SystemStatus.vue、frontend/src/views/DataUpdate.vue |
| **A2** 折叠面板不加载品种 | `el-collapse` 绑定 `@change`，展开某模块时才拉取该模块 `varieties`；增加加载提示与空态文案；默认展开模块仍于 onMounted 预加载。 | frontend/src/views/DataManagement.vue |
| **A3** 死函数清理 | 移除 `news.get_contract_news/get_market_sentiment`、`scheduled_service.should_catch_up`、`commodity_service.get_symbols_by_exchange`。settings 的路径/品种小工具与 models 中未用作 schema 的类型**保留**（作为内部通用工具/未来兼容，风险更低）。 | backend/modules/news.py、backend/services/scheduled_service.py、backend/services/commodity_service.py |
| **A4** 定时后台自动分析 | 真正实现 `update_data_before_analysis`（按模块+品种先更新本地数据）、`auto_word`（生成并保存 Word 到 `RESULTS_DIR/auto_reports`）、`auto_email`（新增 smtplib 邮件服务，`SMTP_*` 为普通配置项、从 `backend/.env` 经 settings 读取，支持 SSL/STARTTLS，附报告）。新增任务完成钩子机制，分析结束后、队列取出下一任务前触发后置动作；`run_scheduled_task.py` 提供无人值守单次执行入口（cron / Windows 任务计划程序）。`use_realtime` 无真实数据源，已从前端 UI 移除（后端字段保留以兼容旧配置/请求）。 | backend/services/scheduled_service.py、backend/services/email_service.py(新)、backend/services/analysis_service.py、backend/run_scheduled_task.py(新)、frontend/src/views/AnalysisConfig.vue |
| **B7** 结果/报告缓存兜底 | `/api/analysis/results/{commodity}` 失败后先找当日缓存、再回退该品种最近一次缓存（`load_latest_cache`）；Word 报告内存结果为空时回退最近缓存生成。`include_charts` 插图能力与 `/api/data/dominant-contracts` 口径统一作为后续增强项保留。 | backend/services/cache_service.py、backend/routers/analysis.py |
| 环境变量说明 | `.env.example`（根与 backend）补充 `TIAN_API_KEY` 说明；`SMTP_*` 作为普通配置项直接写在 `backend/.env`（settings 读取，无需系统环境变量），Docker 部署经 compose 从仓库根 `.env` 透传。 | .env.example、backend/.env.example、docker-compose.yml、backend/core/settings.py、backend/services/email_service.py |
| **B8** 大商所持仓排名取数失败（官网瑞数反爬 412 + 新浪停更） | 接入**东方财富数据中心** `RPT_FUTU_DAILYPOSITION` 作为首选数据源：该接口无鉴权、无动态反爬，按“会员×合约×交易日”返回明细且自带官方三榜名次（`VOLUME_RANK`/`LP_RANK`/`SP_RANK`，9999=未进前 20），与官网发布口径一致。改动：① `_fetch_dce_data_with_fallback` 策略栈改为“东财数据中心 → 官网主接口 → 官网备用接口 → 官网排名表接口”四级降级，并识别 zip/412 类异常给出“官网疑似动态反爬”的提示；② 新增 `_fetch_dce_data_from_eastmoney`（positioning_updater.py:760，按“日期+品种”一次取当日全部合约，分页+请求间隔）、`_query_em_daily_position`（:677，`datacenter-web.eastmoney.com/api/data/v1/get`，columns 取三榜名次与数值）、`_dce_merged_df_from_em`（:711，把明细转成与 `futures_dce_position_rank` 同构的“三榜按名次 1-20 逐行对齐”DataFrame）；③ `_process_contract_positioning_data`（:467）增加空白会员简称跳过，防止某榜单不足 20 名时写空行。验证：2026-08-31 ~ 09-07 六个交易日 JD 每日 5~6 个合约，多单/空单/成交量三榜各 20 条完整入库。 | backend/modules/positioning_updater.py |
| **B3 追修** DCE 期限结构仍取不到数（官网全站瑞数反爬 412，`get_futures_daily(DCE)` 报 `Expecting value: line 1 column 1 (char 0)`，与 B8 同根因） | `fetch_exchange_data` 保留官网 `ak.get_futures_daily` 主通道；当 DCE 官网失败（HTTP 412/空响应）时自动降级到**新浪单合约日K 组装全合约日报**：① `_fetch_dce_daily_from_sina`（按东财静态目录 `futsse-static.eastmoney.com/redis` msgid=114 推导各 DCE 品种当前挂牌合约与交割月份规律，向前补区间内已交割月份、向后补到最远挂牌月，避免逐月盲扫）；② `_sina_contract_kline` 抓取单合约日K（新浪保留已交割合约历史，含 close/volume/持仓量），按窗口日期过滤后聚合成与官网同构的 symbol/date/close/volume/open_interest 全合约日报，再走原有 process→metrics→save 管线；③ `save_variety_data` 清理旧版误存的 `XX0` 主力连续脏行：一旦本次拿到真实合约行即剔除并整表重写，防止“主连单行+真实多合约”混存。验证：EG/JD 降级源能组装 2026-08-26~09-01 连续交易日 13 个合约/日、roll_yield 正常（≠0），已交割近月（EG2608 等）历史完整。 | backend/modules/term_structure_updater.py |
